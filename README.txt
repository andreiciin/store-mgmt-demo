-------------------------  store management tool --------------------------

aici descriu pasii urmati pentru dezvoltarea aplicatiei:

1. am folosit baza in memory H2

    // fa repo si service/controller pt fiec entity, filtreaza dupa role
	// dupa ce faci repo service fa unit tests pt fiec clasa
	// ++ security
	// ++ Write unit tests, at least for one class
	// ++ error mechanism and handling plus logging
	// ++ small Readme to document the project